//- create by afterloe on 2017-3-2 10:24:10 - MIT

include header

link(href= static + "css/portal/platform.css?time=" + __time rel="stylesheet")
//- link(href= static + "css/portal/workflowInfo.css?time=" + __time rel="stylesheet")

style.
  @media (min-width:769px) {
    .change {
      display: inline-block;
      width: 100px;
      height: 50px;
      cursor: pointer;
      float: right;
    }

    .data li div span.local {
      width: 650px;
    }

    .svn {
        background: url(http://almcloud.jwis.cn/images/portal/productTestStatus2.png) no-repeat center 13px;
    }

    .cloud {
        background: url(http://almcloud.jwis.cn/images/portal/productTestStatus4.png) no-repeat center 13px;
    }
  }

  @media (max-width:768px) {
    .change {
      cursor: pointer;
      float: right;
      height: 0.5rem;
      line-height: 0.5rem;
      width: 0.4rem;
      background-size: contain;
      margin-right: 0.4rem
    }

    .svn {
        background: url(http://almcloud.jwis.cn/images/portal/productTestStatus2.png) no-repeat center 0.05rem;
    }

    .cloud {
        background: url(http://almcloud.jwis.cn/images/portal/productTestStatus4.png) no-repeat center 0.05rem;
    }
  }

- const _members = locals.members || [];
- const _process = locals.nodeList || [];
- const _activeNode = locals.status || {};

div(class="container platform_content")
  div(class="content_one")
    div(class="tru")
      div(class="truTitle pc")=`${locals.name} 详情`
      div(class="truContent")
        div(class="truBox")
          div 立项时间
          ul
            li
              span(class="aLink")= new Date(locals.createTimestamp).toLocaleString()
        div(class="truBox")
          div 项目负责人
          ul
            li(data-id="456")
              span(class="aLink")= locals.owner
        div(class="truBox")
          div 参与人员
          ul
            each __member in _members
              li(data-id=__member._id)
                span(class="aLink")=__member.name

  div(class="proposal")

  div(class="wrapper")
  if _activeNode
    div(class="tab_process")
      each _p,index in _process
        dl(class= _activeNode.index === _p.index? "processActive": "")
          dt(data-id=_p._id onClick="javascript:loadNodeInstance(this);")
            a(href="javascript:void(0);" class="tabIcon tabIcon_plan")
            span=_p.name
          dd &bull;&bull;&bull;&bull;&bull;&bull;&bull;&bull;&bull;
          div(class="clear")
    div(class="tab_content")
      div(class="dataBox")
        div(class="header")
          span(class="data_title") 节点信息
        ul(class="data")
          li
            div
              i
              span(class="name") 节点名
              span(class="local")=_activeNode.name
          li
            div
              i
              span(class="name") 该节点负责人
              span(class="local")=_activeNode.owner
          li
            div
              i
              span(class="name") 进入节点时间
              span(class="local")=new Date(_activeNode.beginTimestamp).toLocaleString()
          li
            div
              i
              span(class="name") 节点更新次数
              span(class="local")=_activeNode.uploadCount
        div(class="header")
          span(class="data_title") 资料仓库
          span(class="change cloud")
        ul(class="data")
          li
            div
              i
              span(class="name") svn地址
              span(class="local")
                a(href=_activeNode.svn)=_activeNode.svn

include footer

script(src= static + "js/portal/rem.js?time=" + __time)
//- script(src= static + "js/portal/bin/workflowInfo.js?time=" + __time)

script.

  $(function() {
      //进入该页面，content_one从上往下运动
      const width = $(window).width();
      if (width > 768) {
          var contentOne = $(".content_one").height();
          var height = -contentOne;
          $(".content_one").css("margin-top", height);
          $(".content_one").animate({
              "margin-top": 0
          }, 1000, function() {
              $(".content_one").css("margin-top", 0);
          });
      } else {
          $(".content_one").css("margin-top", 0);
      }
  });

  const cleanSelectd = (nodeName) => {
    $("dl").attr("class", "");
  };

  const loadNodeInstance = btn => {
    const instanceId = $(btn).attr("data-id");
    $.ajax({
      type: "GET",
      url: `/workflow/${_id}/simple`,
      dataType: "json",
      beforeSend: xhr => xhr.setRequestHeader("accept","application/json"),
      success: result => {
        if (401.5 === result.code) {
          alert("登录许可已失效，请重新获取登录许可");
          location.href = "/portal/login";
          return ;
        }
        if (200 !== result.code) {
          alert("服务器繁忙");
        } else {
          cleanSelect();
          $(btn).find("span").css({color: "#3d9bff"});
          console.log(result);
        }
      }
    });
  };

  // 点击反馈跳转到bbs
  $(".proposal").bind("click", function() {
      window.open("http://bbs.jwis.cn/forum.php?mod=forumdisplay&fid=37");
  });
